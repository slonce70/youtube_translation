# Generated from docker/Caddyfile.template via scripts/render_caddyfile.py (--variant __VARIANT__).
# Edit the template, then re-render instead of editing generated outputs by hand.

__PRIMARY_SITE__ {
    handle /api/internal/* {
        respond 404
    }

    handle /api/* {
        reverse_proxy __PRIMARY_BACKEND_UPSTREAM__
    }

    handle /thumbnails/* {
        reverse_proxy __PRIMARY_BACKEND_UPSTREAM__
    }

    handle /files/* {
        reverse_proxy __PRIMARY_TUSD_UPSTREAM__
    }

    handle_path /uploads/* {
        redir /files{path} 308
    }

    handle {
        reverse_proxy __PRIMARY_FRONTEND_UPSTREAM__
    }

    encode gzip

    header {
        __PRIMARY_HSTS_LINE__
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        X-XSS-Protection "1; mode=block"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "camera=(), geolocation=(), microphone=(), payment=(), usb=()"
        Content-Security-Policy "default-src 'self'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; img-src 'self' data: https: blob:; font-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval'; connect-src 'self' https://*.supabase.co wss://*.supabase.co wss:; media-src 'self' blob: https:"
    }

    __PRIMARY_LOG_BLOCK__
}

__SECONDARY_SITE__ {
    handle /api/internal/* {
        respond 404
    }

    handle /api/* {
        reverse_proxy __SECONDARY_BACKEND_UPSTREAM__
    }

    handle /thumbnails/* {
        reverse_proxy __SECONDARY_BACKEND_UPSTREAM__
    }

    handle /files/* {
        reverse_proxy __SECONDARY_TUSD_UPSTREAM__
    }

    handle_path /uploads/* {
        redir /files{path} 308
    }

    handle {
        reverse_proxy __SECONDARY_FRONTEND_UPSTREAM__
    }

    encode gzip

    header {
        __SECONDARY_HSTS_LINE__
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        X-XSS-Protection "1; mode=block"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "camera=(), geolocation=(), microphone=(), payment=(), usb=()"
        Content-Security-Policy "default-src 'self'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; img-src 'self' data: https: blob:; font-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval'; connect-src 'self' https://*.supabase.co wss://*.supabase.co wss:; media-src 'self' blob: https:"
    }

    __SECONDARY_LOG_BLOCK__
}
